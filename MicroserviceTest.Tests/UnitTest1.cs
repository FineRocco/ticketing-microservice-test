    using Xunit;

    // A simple test class to ensure the basics are working.
    public class SmokeTests
    {
        [Fact]
        public void Math_works_as_expected()
        {
            // A basic "smoke test" to confirm the test runner is configured
            // and can execute assertions correctly.
            Assert.Equal(4, 2 + 2);
        }
    }
    

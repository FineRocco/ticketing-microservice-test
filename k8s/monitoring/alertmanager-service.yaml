apiVersion: v1
kind: Service
metadata:
  name: alertmanager-service
  namespace: monitoring
spec:
  type: ClusterIP # Só precisa de ser acessível dentro do cluster
  selector:
    app: alertmanager
  ports:
  - name: web
    port: 9093
    targetPort: 9093